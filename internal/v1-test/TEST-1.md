# Scope

This file is about tests performed by humans

# Test Item - Frontend

1. Trying to disable default account returns "account not found"
2. I clicked on "Roles" and the /admin/roles is empty, I tried going back to admin/users, but it is empty. If I refresh the page in a path that is not /admin/roles, the content re-appears
3. Inside Audit Events, multiple refreshes in audit pollute the list, I want to show when people try to use the audit log, but make the default filter exclude /api/v1/audit
4. Inside Audit Events, ACTOR it's showing the ID, Can we show the actor username and allow clicking on this username to the user details page
5. User details page is not working, it should show roles, permissions, sessions for that user, etc. Only admins should be able to see user details or the own user (he can see his details, but no actions are enabled)
6. I changed settings in authentication settings and it did not show the before and after in the Audit Log
7. Audit log shows METHOD + ENDPOINT, but does not show a Description of the action, include it
8. All fields that accept roles (one or more) should open a in which we can selected/toggle the roles, instead of being text fields prone to errors
9. Audit events should show a "Details" button in which it opens a dialog/modal with the input/output of the request associated with that log. If there was a traceback, it should also show in this modal. Audit events table should include tracebacks

# TODO

Implement one row at a time. Test each change, commit it, and record the commit hash.

| Task | Test item | How to fix | How to test | Commit hash | Test result |
|---|---|---|---|---|---|
| [x] Report immutable system administrators correctly | 1 | Map the system-administrator error to a stable conflict or forbidden response. Mark the account as immutable in the user response and hide or disable its disable action. Keep normal-user disable behavior unchanged. | Disable a bootstrap or configured system administrator and verify the response is not “account not found.” Disable a normal user and verify the user becomes disabled. | 16da536 | PASS — focused Go tests: 3 passed; `npm run build`: passed. |
| [x] Keep Users and Roles data across client-side navigation | 2 | Allow `GET /api/v1/admin/roles` with `roles.read`, keep mutations behind `roles.manage`, and make both admin queries show loading, error, empty, and success states correctly after route changes. | Navigate repeatedly between Users and Roles without a full reload. Verify existing rows remain visible, read-only users can read both lists, and failed requests show an error instead of an empty list. | cef9545 | PASS — focused Go tests: 3 passed; `npm test -- --run`: 28 files/44 tests passed; `npm run build`: passed. |
| [x] Exclude audit-read events from the default audit view | 3 | Keep audit-read events for traceability. Add a server-side exclusion filter for `/api/v1/audit` and enable it by default in the frontend. Let users clear the filter to inspect those events. | Refresh Audit Events several times. Verify the default list does not grow with audit-read events. Clear the exclusion and verify the audit-read events are available. | e3803e9 | PASS — focused Go audit test: 1 passed; focused frontend audit test: 1 passed; `npm run build`: passed. |
| [x] Resolve audit actors to usernames | 4 | Add actor display data to the audit response. Show the username or email in the Actor column and link it to the user-detail route. Keep system actors and deleted users readable with a safe fallback. | Create an event for a known user and for a system actor. Verify the known actor displays a username and opens the permitted user details page; verify the fallback remains usable. | 5eea599 | PASS — focused Go actor/audit tests: 2 passed; focused frontend audit test: 1 passed; `npm run build`: passed. |
| [x] Add an authorized user-detail page | 5 | Add a user-detail endpoint and route that return roles, role sources, permissions, login methods, identities, and sessions. Permit administrators to view any user and users to view only themselves. Disable actions on the self view and reject other unauthorized targets. | Open an administrator, another user, and the current user. Verify the correct details, `403` for an unauthorized target, `404` for an unknown user, and no self-management actions. | 12075eb | PASS — focused Go details/OpenAPI tests: 2 passed; `npm test -- --run`: 28 files/44 tests passed; `npm run build`: passed. |
| [x] Record authentication-settings before and after values | 6 | Read the current settings before a successful update and write one audit event containing redacted `before` and `after` values. Do not record an update as successful when validation fails. | Change password login, registration, and default role settings. Verify the audit event contains the previous and new values, while failed changes do not claim success. | d8e5466 | PASS — `go test ./...`: 198 passed in 11 packages; `npm test -- --run`: 28 files/44 tests passed; `npm run build`: passed. |
| [x] Add human-readable audit descriptions | 7 | Add a route or action description to audit events. Populate it from a shared action catalog, while retaining method and endpoint fields for diagnostics. | Trigger authentication, user, role, and scheduler actions. Verify the table shows a useful description and still shows the method and endpoint. | ab88878 | PASS — focused Go audit tests: 3 passed; focused frontend audit test: 1 passed; `npm run build`: passed. |
| [x] Replace role text inputs with role selectors | 8 | Load available roles and use accessible native select or checkbox controls for every single- or multi-role field. Submit role keys from selected options, not free-form text. | Check authentication default-role and SSO group-role fields. Select, clear, keyboard-toggle, and save roles. Verify the request contains only selected existing role keys. | 7bfda8a | PASS — focused selector test: 1 passed; `npm run build`: passed. |
| [ ] Add audit event details and traceback display | 9 | Extend the audit contract with request input, response output, and traceback fields. Redact secrets and render values as text. Add a Details dialog and a traceback column or indicator in the table. | Open Details for events with input, output, traceback, and redacted secrets. Verify safe rendering, long values, missing values, and traceback visibility without HTML execution. | |
