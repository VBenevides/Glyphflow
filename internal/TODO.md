# Glyphflow implementation roadmap

This roadmap converts the local scheduler into a secure network orchestration platform.

The source is the local script scheduler's Network Scheduler Migration Report. All items remain unchecked until implementation and verification are complete.

## Project outcome

Glyphflow will have one central control plane and many workers on remote virtual machines.

The control plane will own all central data and task state changes. Workers will receive signed orders and return signed events.

PostgreSQL will be the source of truth. NATS JetStream will provide durable, at-least-once message delivery.

## Phase 0: Project foundation

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [x] Confirm the MVP boundary | Limit the MVP to one control plane, one queue, and remote workers. | Prevents premature service splitting and reduces the first release risk. | In the repository documentation and root configuration: limit the MVP to one control plane, one queue, and remote workers. | Review the repository and run its documented startup steps; verify this requirement: Limit the MVP to one control plane, one queue, and remote workers. | commit: 456b857; PASS — README documents one control plane, one queue, and outbound-only workers. |
| [x] Define project terms | Use `control plane`, `producer`, `worker`, `order`, `event`, and `task run` consistently. | Prevents protocol and documentation conflicts. | In the repository documentation and root configuration: use `control plane`, `producer`, `worker`, `order`, `event`, and `task run` consistently. | Review the repository and run its documented startup steps; verify this requirement: Use `control plane`, `producer`, `worker`, `order`, `event`, and `task run` consistently. | commit: 3807469; PASS — internal/TERMS.md defines all six terms. |
| [x] Define repository layout | Reserve `backend/` for Go and `frontend/` for the TypeScript and React application. | Gives contributors a stable project structure. | In the repository documentation and root configuration: reserve `backend/` for Go and `frontend/` for the TypeScript and React application. | Review the repository and run its documented startup steps; verify this requirement: Reserve `backend/` for Go and `frontend/` for the TypeScript and React application. | commit: a27e9b2; PASS — internal/REPOSITORY.md documents the backend, frontend, internal, local, and assets boundaries. |
| [x] Create the Go backend module | Create one Go module for shared protocol code, the control plane, and the worker. | Allows both executables to share tested message types. | In the repository documentation and root configuration: create one Go module for shared protocol code, the control plane, and the worker. | Review the repository and run its documented startup steps; verify this requirement: Create one Go module for shared protocol code, the control plane, and the worker. | commit: c0b6c55; PASS — `GOCACHE=/tmp/glyphflow-go-cache go test ./...` completed successfully. |
| [x] Create the default TypeScript and React frontend | Use the standard React TypeScript template without extra application frameworks. | Provides a familiar frontend with low maintenance cost. | In the repository documentation and root configuration: use the standard React TypeScript template without extra application frameworks. | Review the repository and run its documented startup steps; verify this requirement: Use the standard React TypeScript template without extra application frameworks. | commit: 0cdc80c; PASS — `npm run build` completed successfully in `frontend/`. |
| [x] Add local development services | Define local PostgreSQL and NATS JetStream services. | Makes development and integration tests reproducible. | In the repository documentation and root configuration: define local PostgreSQL and NATS JetStream services. | Review the repository and run its documented startup steps; verify this requirement: Define local PostgreSQL and NATS JetStream services. | commit: ea0c83c; PASS — `docker compose config -q` accepted the PostgreSQL and JetStream services. |
| [x] Add configuration validation | Validate required addresses, paths, limits, and identities during startup. | Stops unsafe or incomplete deployments before work starts. | In the repository documentation and root configuration: validate required addresses, paths, limits, and identities during startup. | Review the repository and run its documented startup steps; verify this requirement: Validate required addresses, paths, limits, and identities during startup. | commit: f218980; PASS — `GOCACHE=/tmp/glyphflow-go-cache go test ./...` passed 2 config tests and valid control-plane startup. |
| [x] Record architecture decisions | Document protocol, database, queue, and security decisions. | Keeps future changes consistent with the system model. | In the repository documentation and root configuration: document protocol, database, queue, and security decisions. | Review the repository and run its documented startup steps; verify this requirement: Document protocol, database, queue, and security decisions. | commit: 86f91c2; PASS — internal/DECISIONS.md records protocol, database, queue, and security decisions. |
| [x] Define the supported platform matrix | Select the first Linux and Windows architectures for workers. | Sets clear build, installer, and test requirements. | In the repository documentation and root configuration: select the first Linux and Windows architectures for workers. | Review the repository and run its documented startup steps; verify this requirement: Select the first Linux and Windows architectures for workers. | commit: 3aecd79; PASS — worker cross-builds succeeded for Linux amd64, Linux arm64, and Windows amd64. |

Exit condition: contributors can start the empty backend, frontend, PostgreSQL, and JetStream development environment.

## Phase 1: Shared protocol

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [x] Define the outer envelope | Add `key_id`, base64 payload bytes, and a base64 Ed25519 signature. | Gives orders and events one verifiable transport format. | In `backend/internal/protocol`: add `key_id`, base64 payload bytes, and a base64 Ed25519 signature. | Run protocol unit and golden-vector tests; verify this requirement: Add `key_id`, base64 payload bytes, and a base64 Ed25519 signature. | commit: b20e275; PASS — protocol envelope round-trip test preserves key ID and exact base64-decoded payload bytes. |
| [x] Sign exact payload bytes | Sign stored payload bytes instead of rebuilt JSON. | Removes canonical JSON differences from signature verification. | In `backend/internal/protocol`: sign stored payload bytes instead of rebuilt JSON. | Run protocol unit and golden-vector tests; verify this requirement: Sign stored payload bytes instead of rebuilt JSON. | commit: 888dff1; PASS — signature test rejects a payload-byte mutation. |
| [x] Add signature domains | Use separate fixed domains for orders and events. | Prevents an event signature from authorizing an order. | In `backend/internal/protocol`: use separate fixed domains for orders and events. | Run protocol unit and golden-vector tests; verify this requirement: Use separate fixed domains for orders and events. | commit: e5660a5; PASS — order signatures fail event-domain verification. |
| [x] Fix the signature algorithm | Accept Ed25519 only and ignore untrusted algorithm choices. | Prevents algorithm downgrade and confusion attacks. | In `backend/internal/protocol`: accept Ed25519 only and ignore untrusted algorithm choices. | Run protocol unit and golden-vector tests; verify this requirement: Accept Ed25519 only and ignore untrusted algorithm choices. | commit: 26f6c0f; PASS — protocol exposes a fixed Ed25519 algorithm and signs/verifies without an algorithm selector. |
| [x] Define the order payload | Include identifiers, version, attempt, lease, times, runner, command arguments, timeout, secrets, limits, and resources. | Gives workers all required execution data without database access. | In `backend/internal/protocol`: include identifiers, version, attempt, lease, times, runner, command arguments, timeout, secrets, limits, and resources. | Run protocol unit and golden-vector tests; verify this requirement: Include identifiers, version, attempt, lease, times, runner, command arguments, timeout, secrets, limits, and resources. | commit: eca56bc; PASS — order payload JSON round-trip preserves identifiers, timing, command, secret references, limits, and resources. |
| [x] Define the event payload | Include identifiers, type, attempt, lease, runner, sequence, time, result, metrics, digest, and bounded errors. | Gives the control plane enough data to validate each state change. | In `backend/internal/protocol`: include identifiers, type, attempt, lease, runner, sequence, time, result, metrics, digest, and bounded errors. | Run protocol unit and golden-vector tests; verify this requirement: Include identifiers, type, attempt, lease, runner, sequence, time, result, metrics, digest, and bounded errors. | commit: 549eccd; PASS — event payload JSON round-trip preserves lifecycle identifiers, sequence, metrics, digest, and result fields. |
| [x] Define order types | Support execution and cancellation as signed order types. | Prevents cancellation from using an unverified side channel. | In `backend/internal/protocol`: support execution and cancellation as signed order types. | Run protocol unit and golden-vector tests; verify this requirement: Support execution and cancellation as signed order types. | commit: f2c1b62; PASS — execute and cancel are accepted; unsupported pause type is rejected. |
| [x] Define event types | Support accepted, rejected, started, heartbeat, completed, failed, timed out, and cancelled events. | Creates a complete task lifecycle. | In `backend/internal/protocol`: support accepted, rejected, started, heartbeat, completed, failed, timed out, and cancelled events. | Run protocol unit and golden-vector tests; verify this requirement: Support accepted, rejected, started, heartbeat, completed, failed, timed out, and cancelled events. | commit: 612b1b9; PASS — all eight lifecycle event types are accepted and an unsupported type is rejected. |
| [x] Verify before parsing payloads | Parse only the small envelope before signature verification. | Reduces exposure to forged payloads. | In `backend/internal/protocol`: parse only the small envelope before signature verification. | Run protocol unit and golden-vector tests; verify this requirement: Parse only the small envelope before signature verification. | commit: b5afa60; PASS — signed non-JSON payload is verified and returned without payload parsing. |
| [x] Add message size limits | Reject oversized envelopes before base64 decoding or JSON parsing. | Controls memory use and denial-of-service risk. | In `backend/internal/protocol`: reject oversized envelopes before base64 decoding or JSON parsing. | Run protocol unit and golden-vector tests; verify this requirement: Reject oversized envelopes before base64 decoding or JSON parsing. | commit: 680152d; PASS — limited decode rejects oversized input before JSON/base64 processing. |
| [x] Add protocol version checks | Reject unsupported protocol versions and message types. | Makes incompatible upgrades fail safely. | In `backend/internal/protocol`: reject unsupported protocol versions and message types. | Run protocol unit and golden-vector tests; verify this requirement: Reject unsupported protocol versions and message types. | commit: 98e29d8; PASS — envelope decode rejects unsupported protocol versions. |
| [x] Add time validation | Validate issue, not-before, expiration, observation, and clock tolerance values. | Limits replay and catches clock problems. | In `backend/internal/protocol`: validate issue, not-before, expiration, observation, and clock tolerance values. | Run protocol unit and golden-vector tests; verify this requirement: Validate issue, not-before, expiration, observation, and clock tolerance values. | commit: 0db94e5; PASS — order expiry and future event observation are rejected with clock tolerance applied. |
| [ ] Add identity validation | Check the assigned runner, run identifier, attempt, and lease token. | Stops one worker from applying another worker's work. | In `backend/internal/protocol`: check the assigned runner, run identifier, attempt, and lease token. | Run protocol unit and golden-vector tests; verify this requirement: Check the assigned runner, run identifier, attempt, and lease token. | |
| [ ] Add sequence validation | Require the expected event sequence for each attempt. | Stops skipped or reordered state transitions. | In `backend/internal/protocol`: require the expected event sequence for each attempt. | Run protocol unit and golden-vector tests; verify this requirement: Require the expected event sequence for each attempt. | |
| [ ] Add golden protocol vectors | Store cross-executable signing and verification examples. | Detects accidental wire-format changes. | In `backend/internal/protocol`: store cross-executable signing and verification examples. | Run protocol unit and golden-vector tests; verify this requirement: Store cross-executable signing and verification examples. | |
| [ ] Test altered payload rejection | Change one payload byte and require verification failure. | Proves message integrity. | In `backend/internal/protocol`: change one payload byte and require verification failure. | Run protocol unit and golden-vector tests; verify this requirement: Change one payload byte and require verification failure. | |
| [ ] Test wrong-key and revoked-key rejection | Reject unknown, invalid, expired, or revoked signing keys. | Proves key policy enforcement. | In `backend/internal/protocol`: reject unknown, invalid, expired, or revoked signing keys. | Run protocol unit and golden-vector tests; verify this requirement: Reject unknown, invalid, expired, or revoked signing keys. | |
| [ ] Test replay and wrong-runner rejection | Reuse identifiers and change the assigned runner in test messages. | Proves the main authorization checks. | In `backend/internal/protocol`: reuse identifiers and change the assigned runner in test messages. | Run protocol unit and golden-vector tests; verify this requirement: Reuse identifiers and change the assigned runner in test messages. | |
| [ ] Test key rotation overlap | Accept old and new valid keys during a defined rotation window. | Allows rotation without message loss. | In `backend/internal/protocol`: accept old and new valid keys during a defined rotation window. | Run protocol unit and golden-vector tests; verify this requirement: Accept old and new valid keys during a defined rotation window. | |

Exit condition: both executables reject altered, expired, duplicate, and wrong-runner messages.

## Phase 2: PostgreSQL state

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Add versioned database migrations | Apply ordered PostgreSQL schema changes during deployment. | Makes database upgrades repeatable and reviewable. | In `backend/migrations` and the PostgreSQL store package: apply ordered PostgreSQL schema changes during deployment. | Run PostgreSQL migration and integration tests; verify this requirement: Apply ordered PostgreSQL schema changes during deployment. | |
| [ ] Create `task_definitions` | Store schedules, versions, commands, time zones, selectors, resources, and retry policy. | Creates the central task configuration. | In `backend/migrations` and the PostgreSQL store package: store schedules, versions, commands, time zones, selectors, resources, and retry policy. | Run PostgreSQL migration and integration tests; verify this requirement: Store schedules, versions, commands, time zones, selectors, resources, and retry policy. | |
| [ ] Create `task_runs` | Store each occurrence, assignment, state, attempt, lease, timestamps, and result. | Creates the authoritative execution state. | In `backend/migrations` and the PostgreSQL store package: store each occurrence, assignment, state, attempt, lease, timestamps, and result. | Run PostgreSQL migration and integration tests; verify this requirement: Store each occurrence, assignment, state, attempt, lease, timestamps, and result. | |
| [ ] Prevent duplicate occurrences | Add a unique constraint for each task definition and scheduled occurrence. | Stops multiple producer replicas from creating the same run. | In `backend/migrations` and the PostgreSQL store package: add a unique constraint for each task definition and scheduled occurrence. | Run PostgreSQL migration and integration tests; verify this requirement: Add a unique constraint for each task definition and scheduled occurrence. | |
| [ ] Create `run_events` | Store accepted worker events as an append-only audit history. | Preserves the complete task lifecycle. | In `backend/migrations` and the PostgreSQL store package: store accepted worker events as an append-only audit history. | Run PostgreSQL migration and integration tests; verify this requirement: Store accepted worker events as an append-only audit history. | |
| [ ] Enforce event uniqueness | Add unique constraints for event identifiers and run-attempt sequences. | Makes duplicate events harmless. | In `backend/migrations` and the PostgreSQL store package: add unique constraints for event identifiers and run-attempt sequences. | Run PostgreSQL migration and integration tests; verify this requirement: Add unique constraints for event identifiers and run-attempt sequences. | |
| [ ] Create `runners` | Store worker identity, pool, capacity, capabilities, state, and heartbeat time. | Allows safe worker selection and health reporting. | In `backend/migrations` and the PostgreSQL store package: store worker identity, pool, capacity, capabilities, state, and heartbeat time. | Run PostgreSQL migration and integration tests; verify this requirement: Store worker identity, pool, capacity, capabilities, state, and heartbeat time. | |
| [ ] Create `runner_keys` | Store public keys, key identifiers, validity periods, and revocation data. | Supports signature verification and key lifecycle operations. | In `backend/migrations` and the PostgreSQL store package: store public keys, key identifiers, validity periods, and revocation data. | Run PostgreSQL migration and integration tests; verify this requirement: Store public keys, key identifiers, validity periods, and revocation data. | |
| [ ] Create `runner_enrollments` | Store token hashes, expiry, use, requester, target, and artifact data. | Supports one-use worker enrollment without stored plain tokens. | In `backend/migrations` and the PostgreSQL store package: store token hashes, expiry, use, requester, target, and artifact data. | Run PostgreSQL migration and integration tests; verify this requirement: Store token hashes, expiry, use, requester, target, and artifact data. | |
| [ ] Limit unused enrollments | Allow only one unused enrollment for each runner. | Prevents multiple valid installers for one identity. | In `backend/migrations` and the PostgreSQL store package: allow only one unused enrollment for each runner. | Run PostgreSQL migration and integration tests; verify this requirement: Allow only one unused enrollment for each runner. | |
| [ ] Create `resource_leases` | Store resource ownership, task run, lease token, and expiration. | Prevents conflicting tasks from using exclusive resources. | In `backend/migrations` and the PostgreSQL store package: store resource ownership, task run, lease token, and expiration. | Run PostgreSQL migration and integration tests; verify this requirement: Store resource ownership, task run, lease token, and expiration. | |
| [ ] Create `dispatch_outbox` | Store exact signed order bytes, subjects, and publication state. | Prevents database commits from losing queue publication. | In `backend/migrations` and the PostgreSQL store package: store exact signed order bytes, subjects, and publication state. | Run PostgreSQL migration and integration tests; verify this requirement: Store exact signed order bytes, subjects, and publication state. | |
| [ ] Create `event_inbox` | Store received event identifiers in the state update transaction. | Prevents duplicate events from changing state twice. | In `backend/migrations` and the PostgreSQL store package: store received event identifiers in the state update transaction. | Run PostgreSQL migration and integration tests; verify this requirement: Store received event identifiers in the state update transaction. | |
| [ ] Add state constraints | Restrict task runs and runners to documented states. | Stops invalid values outside application code. | In `backend/migrations` and the PostgreSQL store package: restrict task runs and runners to documented states. | Run PostgreSQL migration and integration tests; verify this requirement: Restrict task runs and runners to documented states. | |
| [ ] Add optimistic or locked updates | Use a state version or row lock for each task state change. | Prevents concurrent event races. | In `backend/migrations` and the PostgreSQL store package: use a state version or row lock for each task state change. | Run PostgreSQL migration and integration tests; verify this requirement: Use a state version or row lock for each task state change. | |
| [ ] Add retention indexes | Index due tasks, pending outbox rows, recent runs, heartbeats, and audit queries. | Keeps core operations efficient as history grows. | In `backend/migrations` and the PostgreSQL store package: index due tasks, pending outbox rows, recent runs, heartbeats, and audit queries. | Run PostgreSQL migration and integration tests; verify this requirement: Index due tasks, pending outbox rows, recent runs, heartbeats, and audit queries. | |
| [ ] Add SQLite import tooling | Import existing task definitions from the local scheduler database. | Preserves current schedules during migration. | In `backend/migrations` and the PostgreSQL store package: import existing task definitions from the local scheduler database. | Run PostgreSQL migration and integration tests; verify this requirement: Import existing task definitions from the local scheduler database. | |
| [ ] Test transactional run creation | Create the task run, leases, and outbox row in one transaction. | Proves that no order exists without durable central state. | In `backend/migrations` and the PostgreSQL store package: create the task run, leases, and outbox row in one transaction. | Run PostgreSQL migration and integration tests; verify this requirement: Create the task run, leases, and outbox row in one transaction. | |

Exit condition: one transaction creates a unique task run, its resource leases, and its outbox record.

## Phase 3: Go control plane and producer

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Create the control plane executable | Run the API, scheduler, dispatcher, event ingester, housekeeping, and notifications in one process. | Delivers the MVP without unnecessary services. | In the control-plane command and package: run the API, scheduler, dispatcher, event ingester, housekeeping, and notifications in one process. | Run control-plane unit and integration tests; verify this requirement: Run the API, scheduler, dispatcher, event ingester, housekeeping, and notifications in one process. | |
| [ ] Add graceful shutdown | Stop HTTP, database, queue, and background loops with bounded waits. | Prevents lost in-process work during deployments. | In the control-plane command and package: stop HTTP, database, queue, and background loops with bounded waits. | Run control-plane unit and integration tests; verify this requirement: Stop HTTP, database, queue, and background loops with bounded waits. | |
| [ ] Implement schedule calculation | Support manual, fixed-time, and cron schedules with explicit time zones. | Preserves local scheduler behavior in the central producer. | In the control-plane command and package: support manual, fixed-time, and cron schedules with explicit time zones. | Run control-plane unit and integration tests; verify this requirement: Support manual, fixed-time, and cron schedules with explicit time zones. | |
| [ ] Lock due occurrences | Use PostgreSQL row locks so one producer claims each due occurrence. | Allows multiple control plane replicas without duplicate runs. | In the control-plane command and package: use PostgreSQL row locks so one producer claims each due occurrence. | Run control-plane unit and integration tests; verify this requirement: Use PostgreSQL row locks so one producer claims each due occurrence. | |
| [ ] Select active workers | Match runner selectors, status, capabilities, lease, and available capacity. | Sends work only to eligible workers. | In the control-plane command and package: match runner selectors, status, capabilities, lease, and available capacity. | Run control-plane unit and integration tests; verify this requirement: Match runner selectors, status, capabilities, lease, and available capacity. | |
| [ ] Reserve exclusive resources | Create resource leases in the task run transaction. | Prevents conflicting tasks from starting together. | In the control-plane command and package: create resource leases in the task run transaction. | Run control-plane unit and integration tests; verify this requirement: Create resource leases in the task run transaction. | |
| [ ] Create task runs transactionally | Store the occurrence, assignment, lease, and outbox record together. | Keeps task state and delivery state consistent. | In the control-plane command and package: store the occurrence, assignment, lease, and outbox record together. | Run control-plane unit and integration tests; verify this requirement: Store the occurrence, assignment, lease, and outbox record together. | |
| [ ] Store signed envelopes before publication | Create and retain the exact order envelope in the outbox. | Makes retries publish identical signed bytes. | In the control-plane command and package: create and retain the exact order envelope in the outbox. | Run control-plane unit and integration tests; verify this requirement: Create and retain the exact order envelope in the outbox. | |
| [ ] Dispatch with `SKIP LOCKED` | Let control plane replicas share pending outbox work. | Adds safe horizontal dispatch capacity. | In the control-plane command and package: let control plane replicas share pending outbox work. | Run control-plane unit and integration tests; verify this requirement: Let control plane replicas share pending outbox work. | |
| [ ] Mark publication after confirmation | Update outbox state only after JetStream confirms the publish. | Prevents silent order loss. | In the control-plane command and package: update outbox state only after JetStream confirms the publish. | Run control-plane unit and integration tests; verify this requirement: Update outbox state only after JetStream confirms the publish. | |
| [ ] Verify worker events | Validate the certificate context, signature, assignment, attempt, lease, and sequence. | Stops forged or stale events from changing task state. | In the control-plane command and package: validate the certificate context, signature, assignment, attempt, lease, and sequence. | Run control-plane unit and integration tests; verify this requirement: Validate the certificate context, signature, assignment, attempt, lease, and sequence. | |
| [ ] Apply allowed transitions only | Implement the documented task run state machine. | Prevents workers from selecting arbitrary states. | In the control-plane command and package: implement the documented task run state machine. | Run control-plane unit and integration tests; verify this requirement: Implement the documented task run state machine. | |
| [ ] Commit before queue acknowledgement | Store the inbox record, event, state change, and lease release before acknowledgement. | Makes event processing recoverable. | In the control-plane command and package: store the inbox record, event, state change, and lease release before acknowledgement. | Run control-plane unit and integration tests; verify this requirement: Store the inbox record, event, state change, and lease release before acknowledgement. | |
| [ ] Keep old-attempt events for audit | Record stale events without changing the current attempt. | Preserves evidence while protecting current state. | In the control-plane command and package: record stale events without changing the current attempt. | Run control-plane unit and integration tests; verify this requirement: Record stale events without changing the current attempt. | |
| [ ] Add runner lease recovery | Mark workers offline and runs lost after defined safety intervals. | Gives operators accurate failure state. | In the control-plane command and package: mark workers offline and runs lost after defined safety intervals. | Run control-plane unit and integration tests; verify this requirement: Mark workers offline and runs lost after defined safety intervals. | |
| [ ] Release resources on final states | Release leases only after a verified final state. | Prevents early resource reuse. | In the control-plane command and package: release leases only after a verified final state. | Run control-plane unit and integration tests; verify this requirement: Release leases only after a verified final state. | |
| [ ] Add housekeeping | Remove expired audit data and abandoned leases by policy. | Controls storage use and stale reservations. | In the control-plane command and package: remove expired audit data and abandoned leases by policy. | Run control-plane unit and integration tests; verify this requirement: Remove expired audit data and abandoned leases by policy. | |
| [ ] Add final-state notifications | Send email only after the control plane verifies a final event. | Keeps SMTP credentials away from workers. | In the control-plane command and package: send email only after the control plane verifies a final event. | Run control-plane unit and integration tests; verify this requirement: Send email only after the control plane verifies a final event. | |
| [ ] Add bounded retry backoff | Retry transient database and queue operations with limits and jitter. | Avoids overload during outages. | In the control-plane command and package: retry transient database and queue operations with limits and jitter. | Run control-plane unit and integration tests; verify this requirement: Retry transient database and queue operations with limits and jitter. | |

Exit condition: the control plane produces, dispatches, ingests, and records one task run without direct worker database access.

## Phase 4: HTTP API and user access

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Define the versioned HTTP API | Add a stable API prefix and documented JSON error format. | Gives the frontend a predictable contract. | In the control-plane HTTP API package: add a stable API prefix and documented JSON error format. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Add a stable API prefix and documented JSON error format. | |
| [ ] Add user authentication | Require a verified user identity for every management endpoint. | Prevents anonymous control of workers and tasks. | In the control-plane HTTP API package: require a verified user identity for every management endpoint. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Require a verified user identity for every management endpoint. | |
| [ ] Add role-based authorization | Separate task, runner, enrollment, key, and audit permissions. | Limits damage from compromised user accounts. | In the control-plane HTTP API package: separate task, runner, enrollment, key, and audit permissions. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Separate task, runner, enrollment, key, and audit permissions. | |
| [ ] Add task definition endpoints | Create, read, update, enable, disable, and version task definitions. | Supports the main frontend workflow. | In the control-plane HTTP API package: create, read, update, enable, disable, and version task definitions. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Create, read, update, enable, disable, and version task definitions. | |
| [ ] Add manual run endpoints | Create immediate task runs through the same producer path. | Avoids a second execution path. | In the control-plane HTTP API package: create immediate task runs through the same producer path. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Create immediate task runs through the same producer path. | |
| [ ] Add run history endpoints | Filter runs by task, runner, state, attempt, and time. | Supports operations and incident review. | In the control-plane HTTP API package: filter runs by task, runner, state, attempt, and time. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Filter runs by task, runner, state, attempt, and time. | |
| [ ] Add run event endpoints | Return the ordered audit timeline for one run. | Shows why and when each state changed. | In the control-plane HTTP API package: return the ordered audit timeline for one run. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Return the ordered audit timeline for one run. | |
| [ ] Add cancellation endpoints | Request cancellation and create a signed cancellation order. | Gives operators a controlled stop mechanism. | In the control-plane HTTP API package: request cancellation and create a signed cancellation order. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Request cancellation and create a signed cancellation order. | |
| [ ] Add retry endpoints | Require an explicit retry-safe decision and create a new attempt. | Reduces duplicate external effects. | In the control-plane HTTP API package: require an explicit retry-safe decision and create a new attempt. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Require an explicit retry-safe decision and create a new attempt. | |
| [ ] Add runner management endpoints | Create, inspect, disable, reset, and revoke runners. | Supports the complete worker lifecycle. | In the control-plane HTTP API package: create, inspect, disable, reset, and revoke runners. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Create, inspect, disable, reset, and revoke runners. | |
| [ ] Add enrollment download permission | Restrict installer creation to authorized users. | Protects worker identities and bootstrap tokens. | In the control-plane HTTP API package: restrict installer creation to authorized users. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Restrict installer creation to authorized users. | |
| [ ] Add input limits and validation | Limit bodies, strings, arrays, timeouts, paths, and selectors. | Protects trust boundaries and database integrity. | In the control-plane HTTP API package: limit bodies, strings, arrays, timeouts, paths, and selectors. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Limit bodies, strings, arrays, timeouts, paths, and selectors. | |
| [ ] Add pagination | Paginate tasks, runs, events, runners, and audit records. | Prevents large responses from degrading the control plane. | In the control-plane HTTP API package: paginate tasks, runs, events, runners, and audit records. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Paginate tasks, runs, events, runners, and audit records. | |
| [ ] Add no-store download headers | Prevent shared caches from storing enrollment artifacts. | Reduces bootstrap token exposure. | In the control-plane HTTP API package: prevent shared caches from storing enrollment artifacts. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Prevent shared caches from storing enrollment artifacts. | |
| [ ] Add API audit records | Record actor, action, target, address, time, and result. | Makes administrative changes traceable. | In the control-plane HTTP API package: record actor, action, target, address, time, and result. | Run HTTP integration tests as authorized and unauthorized users; verify this requirement: Record actor, action, target, address, time, and result. | |

Exit condition: authorized users can manage tasks, workers, and runs without direct database access.

## Phase 5: TypeScript and React frontend

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Create the default React TypeScript application | Start with the standard template and build setup. | Keeps the frontend familiar and easy to maintain. | In `frontend/src` using the existing React structure: start with the standard template and build setup. | Run frontend component and workflow tests; verify this requirement: Start with the standard template and build setup. | |
| [ ] Add typed API models | Define frontend types for tasks, workers, runs, events, and errors. | Catches contract mismatches during development. | In `frontend/src` using the existing React structure: define frontend types for tasks, workers, runs, events, and errors. | Run frontend component and workflow tests; verify this requirement: Define frontend types for tasks, workers, runs, events, and errors. | |
| [ ] Add authentication flow | Handle sign-in, sign-out, session expiry, and forbidden actions. | Protects management functions in the browser. | In `frontend/src` using the existing React structure: handle sign-in, sign-out, session expiry, and forbidden actions. | Run frontend component and workflow tests; verify this requirement: Handle sign-in, sign-out, session expiry, and forbidden actions. | |
| [ ] Add application navigation | Provide pages for tasks, workers, runs, and audit history. | Makes primary operations easy to find. | In `frontend/src` using the existing React structure: provide pages for tasks, workers, runs, and audit history. | Run frontend component and workflow tests; verify this requirement: Provide pages for tasks, workers, runs, and audit history. | |
| [ ] Add task list and filters | Show name, state, schedule, runner selector, and version. | Gives operators a clear task inventory. | In `frontend/src` using the existing React structure: show name, state, schedule, runner selector, and version. | Run frontend component and workflow tests; verify this requirement: Show name, state, schedule, runner selector, and version. | |
| [ ] Add task editor | Edit schedules, argument arrays, paths, timeouts, selectors, resources, and retry policy. | Exposes the full producer configuration safely. | In `frontend/src` using the existing React structure: edit schedules, argument arrays, paths, timeouts, selectors, resources, and retry policy. | Run frontend component and workflow tests; verify this requirement: Edit schedules, argument arrays, paths, timeouts, selectors, resources, and retry policy. | |
| [ ] Validate schedules in the UI | Show time zone and next occurrences before save. | Reduces scheduling mistakes. | In `frontend/src` using the existing React structure: show time zone and next occurrences before save. | Run frontend component and workflow tests; verify this requirement: Show time zone and next occurrences before save. | |
| [ ] Add manual run action | Start an immediate run from a task page. | Supports testing and urgent operations. | In `frontend/src` using the existing React structure: start an immediate run from a task page. | Run frontend component and workflow tests; verify this requirement: Start an immediate run from a task page. | |
| [ ] Add worker list and status | Show enrollment state, capacity, capabilities, heartbeat age, and key state. | Helps operators identify unavailable workers. | In `frontend/src` using the existing React structure: show enrollment state, capacity, capabilities, heartbeat age, and key state. | Run frontend component and workflow tests; verify this requirement: Show enrollment state, capacity, capabilities, heartbeat age, and key state. | |
| [ ] Add runner enrollment action | Create and download the correct installer for an operating system and architecture. | Provides the primary one-download worker setup. | In `frontend/src` using the existing React structure: create and download the correct installer for an operating system and architecture. | Run frontend component and workflow tests; verify this requirement: Create and download the correct installer for an operating system and architecture. | |
| [ ] Add enrollment reset and revocation actions | Require confirmation and show the security effect. | Supports failed installs and compromised workers. | In `frontend/src` using the existing React structure: require confirmation and show the security effect. | Run frontend component and workflow tests; verify this requirement: Require confirmation and show the security effect. | |
| [ ] Add run list and filters | Show state, attempt, assignment, schedule, duration, and result. | Makes active and failed work easy to inspect. | In `frontend/src` using the existing React structure: show state, attempt, assignment, schedule, duration, and result. | Run frontend component and workflow tests; verify this requirement: Show state, attempt, assignment, schedule, duration, and result. | |
| [ ] Add run detail timeline | Show each verified event and state transition. | Gives operators a complete audit view. | In `frontend/src` using the existing React structure: show each verified event and state transition. | Run frontend component and workflow tests; verify this requirement: Show each verified event and state transition. | |
| [ ] Add cancellation and retry actions | Show current eligibility and require confirmation. | Reduces accidental duplicate or destructive work. | In `frontend/src` using the existing React structure: show current eligibility and require confirmation. | Run frontend component and workflow tests; verify this requirement: Show current eligibility and require confirmation. | |
| [ ] Add clear loading and error states | Show API, authorization, validation, and connection failures. | Prevents hidden failures and repeated actions. | In `frontend/src` using the existing React structure: show API, authorization, validation, and connection failures. | Run frontend component and workflow tests; verify this requirement: Show API, authorization, validation, and connection failures. | |
| [ ] Add accessible forms and tables | Support labels, keyboard use, focus, contrast, and screen readers. | Makes core operations available to more users. | In `frontend/src` using the existing React structure: support labels, keyboard use, focus, contrast, and screen readers. | Run frontend component and workflow tests; verify this requirement: Support labels, keyboard use, focus, contrast, and screen readers. | |
| [ ] Add responsive layouts | Keep management pages usable on common screen sizes. | Supports operators outside a desktop-only workflow. | In `frontend/src` using the existing React structure: keep management pages usable on common screen sizes. | Run frontend component and workflow tests; verify this requirement: Keep management pages usable on common screen sizes. | |
| [ ] Add frontend tests | Test critical forms, permissions, and state actions. | Detects regressions in management workflows. | In `frontend/src` using the existing React structure: test critical forms, permissions, and state actions. | Run frontend component and workflow tests; verify this requirement: Test critical forms, permissions, and state actions. | |

Exit condition: a user can enroll a worker, create a task, start it, and inspect its verified history through the frontend.

## Phase 6: JetStream delivery

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Create the order stream | Store `glyphflow.orders.<runner_id>` subjects with durable retention. | Makes orders survive control plane and queue client restarts. | In the shared JetStream configuration and adapter: store `glyphflow.orders.<runner_id>` subjects with durable retention. | Run JetStream integration tests with restart and redelivery; verify this requirement: Store `glyphflow.orders.<runner_id>` subjects with durable retention. | |
| [ ] Create the event stream | Store `glyphflow.events.<runner_id>` subjects with durable retention. | Makes worker results survive control plane outages. | In the shared JetStream configuration and adapter: store `glyphflow.events.<runner_id>` subjects with durable retention. | Run JetStream integration tests with restart and redelivery; verify this requirement: Store `glyphflow.events.<runner_id>` subjects with durable retention. | |
| [ ] Add per-worker durable consumers | Give each worker one pull consumer for its order subject. | Isolates delivery and preserves worker progress. | In the shared JetStream configuration and adapter: give each worker one pull consumer for its order subject. | Run JetStream integration tests with restart and redelivery; verify this requirement: Give each worker one pull consumer for its order subject. | |
| [ ] Match pending delivery to capacity | Set maximum pending orders from the worker parallel task limit. | Prevents workers from accepting excess work. | In the shared JetStream configuration and adapter: set maximum pending orders from the worker parallel task limit. | Run JetStream integration tests with restart and redelivery; verify this requirement: Set maximum pending orders from the worker parallel task limit. | |
| [ ] Use explicit acknowledgements | Acknowledge an order only after durable local storage. | Prevents accepted work from disappearing after a restart. | In the shared JetStream configuration and adapter: acknowledge an order only after durable local storage. | Run JetStream integration tests with restart and redelivery; verify this requirement: Acknowledge an order only after durable local storage. | |
| [ ] Add the shared event consumer group | Let control plane replicas share event ingestion. | Adds safe event processing capacity. | In the shared JetStream configuration and adapter: let control plane replicas share event ingestion. | Run JetStream integration tests with restart and redelivery; verify this requirement: Let control plane replicas share event ingestion. | |
| [ ] Configure delivery limits | Limit message size, attempts, storage, retention, and acknowledgement time. | Bounds queue resource use and poison-message retries. | In the shared JetStream configuration and adapter: limit message size, attempts, storage, retention, and acknowledgement time. | Run JetStream integration tests with restart and redelivery; verify this requirement: Limit message size, attempts, storage, retention, and acknowledgement time. | |
| [ ] Add dead-letter subjects | Move exhausted or invalid messages to an inspectable subject. | Stops endless delivery loops and preserves diagnostics. | In the shared JetStream configuration and adapter: move exhausted or invalid messages to an inspectable subject. | Run JetStream integration tests with restart and redelivery; verify this requirement: Move exhausted or invalid messages to an inspectable subject. | |
| [ ] Add mutual TLS | Require trusted client certificates for all queue connections. | Authenticates clients and encrypts transport traffic. | In the shared JetStream configuration and adapter: require trusted client certificates for all queue connections. | Run JetStream integration tests with restart and redelivery; verify this requirement: Require trusted client certificates for all queue connections. | |
| [ ] Add per-subject permissions | Let a worker subscribe and publish only with its own runner identifier. | Stops cross-worker message access. | In the shared JetStream configuration and adapter: let a worker subscribe and publish only with its own runner identifier. | Run JetStream integration tests with restart and redelivery; verify this requirement: Let a worker subscribe and publish only with its own runner identifier. | |
| [ ] Match certificates to subjects | Derive or validate the runner identity from the certificate. | Prevents configuration from bypassing queue permissions. | In the shared JetStream configuration and adapter: derive or validate the runner identity from the certificate. | Run JetStream integration tests with restart and redelivery; verify this requirement: Derive or validate the runner identity from the certificate. | |
| [ ] Test broker restart recovery | Restart JetStream during order and event publication. | Proves durable delivery behavior. | In the shared JetStream configuration and adapter: restart JetStream during order and event publication. | Run JetStream integration tests with restart and redelivery; verify this requirement: Restart JetStream during order and event publication. | |

Exit condition: one worker completes a task through JetStream with no PostgreSQL route or credentials.

## Phase 7: Go worker

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Create the worker executable | Build a standalone binary with no PostgreSQL package. | Enforces the control plane data boundary. | In the worker command and package: build a standalone binary with no PostgreSQL package. | Run worker integration tests with a temporary SQLite database; verify this requirement: Build a standalone binary with no PostgreSQL package. | |
| [ ] Load one worker identity | Require one runner identifier and one capacity configuration. | Keeps queue permissions and audit ownership clear. | In the worker command and package: require one runner identifier and one capacity configuration. | Run worker integration tests with a temporary SQLite database; verify this requirement: Require one runner identifier and one capacity configuration. | |
| [ ] Connect outbound only | Connect to enrollment and JetStream without an inbound listener. | Simplifies VM firewall rules. | In the worker command and package: connect to enrollment and JetStream without an inbound listener. | Run worker integration tests with a temporary SQLite database; verify this requirement: Connect to enrollment and JetStream without an inbound listener. | |
| [ ] Consume only the assigned subject | Bind the durable consumer to the configured runner identifier. | Prevents accidental cross-worker execution. | In the worker command and package: bind the durable consumer to the configured runner identifier. | Run worker integration tests with a temporary SQLite database; verify this requirement: Bind the durable consumer to the configured runner identifier. | |
| [ ] Verify each order | Apply size, signature, version, identity, time, attempt, and replay checks. | Stops unauthorized or stale commands. | In the worker command and package: apply size, signature, version, identity, time, attempt, and replay checks. | Run worker integration tests with a temporary SQLite database; verify this requirement: Apply size, signature, version, identity, time, attempt, and replay checks. | |
| [ ] Create the local SQLite database | Store orders, message identifiers, execution state, and outgoing events. | Makes worker restarts recoverable. | In the worker command and package: store orders, message identifiers, execution state, and outgoing events. | Run worker integration tests with a temporary SQLite database; verify this requirement: Store orders, message identifiers, execution state, and outgoing events. | |
| [ ] Enforce unique message identifiers | Reject duplicate orders in SQLite. | Prevents duplicate queue delivery from repeating execution. | In the worker command and package: reject duplicate orders in SQLite. | Run worker integration tests with a temporary SQLite database; verify this requirement: Reject duplicate orders in SQLite. | |
| [ ] Store before acknowledgement | Commit an accepted order before acknowledging JetStream. | Prevents task loss between receipt and execution. | In the worker command and package: commit an accepted order before acknowledging JetStream. | Run worker integration tests with a temporary SQLite database; verify this requirement: Commit an accepted order before acknowledging JetStream. | |
| [ ] Publish accepted events | Sign and queue acceptance after durable storage. | Tells the control plane that the worker owns the order. | In the worker command and package: sign and queue acceptance after durable storage. | Run worker integration tests with a temporary SQLite database; verify this requirement: Sign and queue acceptance after durable storage. | |
| [ ] Limit parallel execution | Use configured capacity to start accepted orders. | Protects VM resources and preserves queue backpressure. | In the worker command and package: use configured capacity to start accepted orders. | Run worker integration tests with a temporary SQLite database; verify this requirement: Use configured capacity to start accepted orders. | |
| [ ] Execute argument arrays | Start executables directly without a shell. | Reduces command injection risk. | In the worker command and package: start executables directly without a shell. | Run worker integration tests with a temporary SQLite database; verify this requirement: Start executables directly without a shell. | |
| [ ] Restrict working directories | Allow task paths only below configured roots. | Prevents arbitrary filesystem access. | In the worker command and package: allow task paths only below configured roots. | Run worker integration tests with a temporary SQLite database; verify this requirement: Allow task paths only below configured roots. | |
| [ ] Validate environment names | Accept only valid names and approved secret references. | Prevents malformed environments and secret leakage. | In the worker command and package: accept only valid names and approved secret references. | Run worker integration tests with a temporary SQLite database; verify this requirement: Accept only valid names and approved secret references. | |
| [ ] Resolve secrets safely | Get secret values from an approved service at execution time. | Keeps secret values out of orders and central logs. | In the worker command and package: get secret values from an approved service at execution time. | Run worker integration tests with a temporary SQLite database; verify this requirement: Get secret values from an approved service at execution time. | |
| [ ] Use dedicated service accounts | Run the worker and tasks with restricted operating system identities. | Limits damage from a task or compromised worker. | In the worker command and package: run the worker and tasks with restricted operating system identities. | Run worker integration tests with a temporary SQLite database; verify this requirement: Run the worker and tasks with restricted operating system identities. | |
| [ ] Apply resource limits | Limit process count, memory, CPU, file access, and execution time where supported. | Protects the VM from runaway tasks. | In the worker command and package: limit process count, memory, CPU, file access, and execution time where supported. | Run worker integration tests with a temporary SQLite database; verify this requirement: Limit process count, memory, CPU, file access, and execution time where supported. | |
| [ ] Use separate process groups | Put each task in a process group and stop the full group. | Prevents child processes from surviving cancellation or timeout. | In the worker command and package: put each task in a process group and stop the full group. | Run worker integration tests with a temporary SQLite database; verify this requirement: Put each task in a process group and stop the full group. | |
| [ ] Bound captured output | Limit error text and calculate a digest for full external logs. | Protects queue storage and reduces secret exposure. | In the worker command and package: limit error text and calculate a digest for full external logs. | Run worker integration tests with a temporary SQLite database; verify this requirement: Limit error text and calculate a digest for full external logs. | |
| [ ] Publish lifecycle events | Sign accepted, started, heartbeat, and final events in sequence. | Gives the control plane a complete verified lifecycle. | In the worker command and package: sign accepted, started, heartbeat, and final events in sequence. | Run worker integration tests with a temporary SQLite database; verify this requirement: Sign accepted, started, heartbeat, and final events in sequence. | |
| [ ] Persist the event outbox | Keep signed events until JetStream confirms publication. | Prevents final results from disappearing during outages. | In the worker command and package: keep signed events until JetStream confirms publication. | Run worker integration tests with a temporary SQLite database; verify this requirement: Keep signed events until JetStream confirms publication. | |
| [ ] Recover after restart | Classify unfinished local work without signaling unverified process identifiers. | Avoids killing unrelated processes after PID reuse. | In the worker command and package: classify unfinished local work without signaling unverified process identifiers. | Run worker integration tests with a temporary SQLite database; verify this requirement: Classify unfinished local work without signaling unverified process identifiers. | |
| [ ] Handle cancellation orders | Match run, attempt, lease, and process group before stopping work. | Stops only the intended execution. | In the worker command and package: match run, attempt, lease, and process group before stopping work. | Run worker integration tests with a temporary SQLite database; verify this requirement: Match run, attempt, lease, and process group before stopping work. | |
| [ ] Report worker heartbeats | Publish signed capacity, active count, and health information. | Supports safe scheduling and offline detection. | In the worker command and package: publish signed capacity, active count, and health information. | Run worker integration tests with a temporary SQLite database; verify this requirement: Publish signed capacity, active count, and health information. | |
| [ ] Remove central database configuration | Reject or omit all PostgreSQL settings from the worker. | Makes the database isolation rule testable. | In the worker command and package: reject or omit all PostgreSQL settings from the worker. | Run worker integration tests with a temporary SQLite database; verify this requirement: Reject or omit all PostgreSQL settings from the worker. | |

Exit condition: a worker executes one verified order once and preserves its final event across restart and network failure.

## Phase 8: Enrollment, keys, and installers

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Protect the control plane key | Use a TPM when available or an encrypted root-owned file. | Reduces the risk of forged execution orders. | In the enrollment package and release scripts: use a TPM when available or an encrypted root-owned file. | Run enrollment and installer tests on each supported platform; verify this requirement: Use a TPM when available or an encrypted root-owned file. | |
| [ ] Generate a random enrollment token | Use a cryptographically secure token for each download. | Prevents token guessing. | In the enrollment package and release scripts: use a cryptographically secure token for each download. | Run enrollment and installer tests on each supported platform; verify this requirement: Use a cryptographically secure token for each download. | |
| [ ] Store only the token hash | Never store or log the plain enrollment token. | Reduces exposure after a database or log leak. | In the enrollment package and release scripts: never store or log the plain enrollment token. | Run enrollment and installer tests on each supported platform; verify this requirement: Never store or log the plain enrollment token. | |
| [ ] Expire enrollment tokens | Use a 15-minute default validity period. | Limits replay after an installer leak. | In the enrollment package and release scripts: use a 15-minute default validity period. | Run enrollment and installer tests on each supported platform; verify this requirement: Use a 15-minute default validity period. | |
| [ ] Enforce one successful use | Claim the token in one PostgreSQL transaction. | Stops a second machine from taking the same identity. | In the enrollment package and release scripts: claim the token in one PostgreSQL transaction. | Run enrollment and installer tests on each supported platform; verify this requirement: Claim the token in one PostgreSQL transaction. | |
| [ ] Bind enrollment to the runner | Reject a valid token for a different runner identifier. | Stops token substitution. | In the enrollment package and release scripts: reject a valid token for a different runner identifier. | Run enrollment and installer tests on each supported platform; verify this requirement: Reject a valid token for a different runner identifier. | |
| [ ] Bind optional platform fields | Validate the selected operating system and architecture. | Stops installation with the wrong artifact. | In the enrollment package and release scripts: validate the selected operating system and architecture. | Run enrollment and installer tests on each supported platform; verify this requirement: Validate the selected operating system and architecture. | |
| [ ] Build common worker releases | Build one tested worker artifact for each supported platform. | Avoids compiling a unique worker for every download. | In the enrollment package and release scripts: build one tested worker artifact for each supported platform. | Run enrollment and installer tests on each supported platform; verify this requirement: Build one tested worker artifact for each supported platform. | |
| [ ] Sign worker releases | Sign each common release artifact and publish its digest. | Detects changed or substituted worker binaries. | In the enrollment package and release scripts: sign each common release artifact and publish its digest. | Run enrollment and installer tests on each supported platform; verify this requirement: Sign each common release artifact and publish its digest. | |
| [ ] Create bootstrap data | Include runner, control plane, trust, token, expiry, version, and digest fields. | Gives the installer only the data needed for enrollment. | In the enrollment package and release scripts: include runner, control plane, trust, token, expiry, version, and digest fields. | Run enrollment and installer tests on each supported platform; verify this requirement: Include runner, control plane, trust, token, expiry, version, and digest fields. | |
| [ ] Sign bootstrap data | Use the control plane Ed25519 key for detached bootstrap signatures. | Detects changed enrollment configuration. | In the enrollment package and release scripts: use the control plane Ed25519 key for detached bootstrap signatures. | Run enrollment and installer tests on each supported platform; verify this requirement: Use the control plane Ed25519 key for detached bootstrap signatures. | |
| [ ] Exclude private keys and certificates | Do not place worker private keys or issued certificates in installers. | Keeps worker identity creation on the target VM. | In the enrollment package and release scripts: do not place worker private keys or issued certificates in installers. | Run enrollment and installer tests on each supported platform; verify this requirement: Do not place worker private keys or issued certificates in installers. | |
| [ ] Create Linux and Windows installers | Package the common worker, signatures, and bootstrap data. | Provides one download for each supported platform. | In the enrollment package and release scripts: package the common worker, signatures, and bootstrap data. | Run enrollment and installer tests on each supported platform; verify this requirement: Package the common worker, signatures, and bootstrap data. | |
| [ ] Verify before installation | Verify release and bootstrap signatures before writing service files. | Stops tampered installers from starting. | In the enrollment package and release scripts: verify release and bootstrap signatures before writing service files. | Run enrollment and installer tests on each supported platform; verify this requirement: Verify release and bootstrap signatures before writing service files. | |
| [ ] Generate keys on first start | Create Ed25519 and mutual TLS private keys on the target VM. | Prevents private key transfer from the control plane. | In the enrollment package and release scripts: create Ed25519 and mutual TLS private keys on the target VM. | Run enrollment and installer tests on each supported platform; verify this requirement: Create Ed25519 and mutual TLS private keys on the target VM. | |
| [ ] Protect local keys | Use a TPM when available or a root-owned encrypted file. | Reduces worker key theft. | In the enrollment package and release scripts: use a TPM when available or a root-owned encrypted file. | Run enrollment and installer tests on each supported platform; verify this requirement: Use a TPM when available or a root-owned encrypted file. | |
| [ ] Enroll through HTTPS | Send the token and public keys through a trusted TLS connection. | Protects bootstrap data in transit. | In the enrollment package and release scripts: send the token and public keys through a trusted TLS connection. | Run enrollment and installer tests on each supported platform; verify this requirement: Send the token and public keys through a trusted TLS connection. | |
| [ ] Issue the client certificate | Return the worker certificate, queue address, subjects, trust, and limits. | Enables authenticated queue access without sharing private keys. | In the enrollment package and release scripts: return the worker certificate, queue address, subjects, trust, and limits. | Run enrollment and installer tests on each supported platform; verify this requirement: Return the worker certificate, queue address, subjects, trust, and limits. | |
| [ ] Activate after signed heartbeat | Mark the worker active only after a verified registration heartbeat. | Confirms that the enrolled machine controls its signing key. | In the enrollment package and release scripts: mark the worker active only after a verified registration heartbeat. | Run enrollment and installer tests on each supported platform; verify this requirement: Mark the worker active only after a verified registration heartbeat. | |
| [ ] Delete bootstrap secrets | Remove the token and temporary files after enrollment. | Reduces secret persistence on the VM. | In the enrollment package and release scripts: remove the token and temporary files after enrollment. | Run enrollment and installer tests on each supported platform; verify this requirement: Remove the token and temporary files after enrollment. | |
| [ ] Support interrupted installation | Reuse protected keys after successful enrollment instead of creating new identities. | Makes safe recovery possible. | In the enrollment package and release scripts: reuse protected keys after successful enrollment instead of creating new identities. | Run enrollment and installer tests on each supported platform; verify this requirement: Reuse protected keys after successful enrollment instead of creating new identities. | |
| [ ] Detect clock differences | Stop enrollment with a clear error when clock skew exceeds policy. | Preserves expiration and signature time checks. | In the enrollment package and release scripts: stop enrollment with a clear error when clock skew exceeds policy. | Run enrollment and installer tests on each supported platform; verify this requirement: Stop enrollment with a clear error when clock skew exceeds policy. | |
| [ ] Add key rotation | Allow two active keys during a defined overlap period. | Supports planned rotation without downtime. | In the enrollment package and release scripts: allow two active keys during a defined overlap period. | Run enrollment and installer tests on each supported platform; verify this requirement: Allow two active keys during a defined overlap period. | |
| [ ] Add key and certificate revocation | Reject new messages and remove queue access immediately. | Limits damage from compromised workers or control plane keys. | In the enrollment package and release scripts: reject new messages and remove queue access immediately. | Run enrollment and installer tests on each supported platform; verify this requirement: Reject new messages and remove queue access immediately. | |
| [ ] Add signed worker updates | Verify version, signature, and digest while preserving identity keys. | Allows safe worker maintenance. | In the enrollment package and release scripts: verify version, signature, and digest while preserving identity keys. | Run enrollment and installer tests on each supported platform; verify this requirement: Verify version, signature, and digest while preserving identity keys. | |

Exit condition: one downloaded installer creates an active worker without embedded or transferred private keys.

## Phase 9: Failure handling and recovery

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Stop production during database failure | Do not create orders when durable task state is unavailable. | Prevents untracked execution. | At the relevant durable handoff in the control plane or worker: do not create orders when durable task state is unavailable. | Run the matching fault-injection test; verify this requirement: Do not create orders when durable task state is unavailable. | |
| [ ] Leave events unacknowledged after commit failure | Let JetStream redeliver events after database recovery. | Prevents lost results. | At the relevant durable handoff in the control plane or worker: let JetStream redeliver events after database recovery. | Run the matching fault-injection test; verify this requirement: Let JetStream redeliver events after database recovery. | |
| [ ] Retry pending outbox rows | Resume order publication after broker recovery. | Prevents queued runs from disappearing. | At the relevant durable handoff in the control plane or worker: resume order publication after broker recovery. | Run the matching fault-injection test; verify this requirement: Resume order publication after broker recovery. | |
| [ ] Retry worker event publication | Resume local outbox delivery after network recovery. | Preserves accepted and final events. | At the relevant durable handoff in the control plane or worker: resume local outbox delivery after network recovery. | Run the matching fault-injection test; verify this requirement: Resume local outbox delivery after network recovery. | |
| [ ] Make duplicate orders idempotent | Return the stored state without starting the command again. | Reduces duplicate external effects. | At the relevant durable handoff in the control plane or worker: return the stored state without starting the command again. | Run the matching fault-injection test; verify this requirement: Return the stored state without starting the command again. | |
| [ ] Make duplicate events idempotent | Treat an existing event identifier as success without a second state update. | Keeps control plane state correct. | At the relevant durable handoff in the control plane or worker: treat an existing event identifier as success without a second state update. | Run the matching fault-injection test; verify this requirement: Treat an existing event identifier as success without a second state update. | |
| [ ] Handle poison messages | Terminate or dead-letter invalid messages with bounded diagnostics. | Prevents endless invalid-message delivery. | At the relevant durable handoff in the control plane or worker: terminate or dead-letter invalid messages with bounded diagnostics. | Run the matching fault-injection test; verify this requirement: Terminate or dead-letter invalid messages with bounded diagnostics. | |
| [ ] Mark interrupted execution safely | Report recovery state without trusting stored process identifiers. | Prevents signals to unrelated processes. | At the relevant durable handoff in the control plane or worker: report recovery state without trusting stored process identifiers. | Run the matching fault-injection test; verify this requirement: Report recovery state without trusting stored process identifiers. | |
| [ ] Detect worker loss | Mark expired workers offline and active runs lost after a safety interval. | Gives operators a clear recovery decision. | At the relevant durable handoff in the control plane or worker: mark expired workers offline and active runs lost after a safety interval. | Run the matching fault-injection test; verify this requirement: Mark expired workers offline and active runs lost after a safety interval. | |
| [ ] Gate automatic retry | Require task owners to declare retry safety and idempotency. | Reduces repeated external effects. | At the relevant durable handoff in the control plane or worker: require task owners to declare retry safety and idempotency. | Run the matching fault-injection test; verify this requirement: Require task owners to declare retry safety and idempotency. | |
| [ ] Use a new lease for each attempt | Increment the attempt and replace the lease token. | Stops stale workers from changing a retried run. | At the relevant durable handoff in the control plane or worker: increment the attempt and replace the lease token. | Run the matching fault-injection test; verify this requirement: Increment the attempt and replace the lease token. | |
| [ ] Keep leases after unconfirmed loss | Release resources only after policy permits recovery. | Prevents overlapping use during uncertain worker state. | At the relevant durable handoff in the control plane or worker: release resources only after policy permits recovery. | Run the matching fault-injection test; verify this requirement: Release resources only after policy permits recovery. | |
| [ ] Test every outage boundary | Stop PostgreSQL, JetStream, the control plane, and workers at commit boundaries. | Proves documented recovery behavior. | At the relevant durable handoff in the control plane or worker: stop PostgreSQL, JetStream, the control plane, and workers at commit boundaries. | Run the matching fault-injection test; verify this requirement: Stop PostgreSQL, JetStream, the control plane, and workers at commit boundaries. | |

Exit condition: each supported failure either recovers automatically or stops with a clear and safe operator state.

## Phase 10: Process and network security

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Isolate the PostgreSQL network | Allow connections only from control plane hosts. | Makes worker database access impossible at the network layer. | In deployment policy and the relevant trust boundary: allow connections only from control plane hosts. | Run the security check in an isolated environment; verify this requirement: Allow connections only from control plane hosts. | |
| [ ] Remove inbound worker requirements | Use outbound enrollment and queue connections only. | Reduces the VM attack surface. | In deployment policy and the relevant trust boundary: use outbound enrollment and queue connections only. | Run the security check in an isolated environment; verify this requirement: Use outbound enrollment and queue connections only. | |
| [ ] Synchronize clocks | Configure and monitor time synchronization on all hosts. | Keeps message expiry, leases, and certificates reliable. | In deployment policy and the relevant trust boundary: configure and monitor time synchronization on all hosts. | Run the security check in an isolated environment; verify this requirement: Configure and monitor time synchronization on all hosts. | |
| [ ] Define trust bundles | Manage control plane, queue, and certificate authority trust separately. | Supports safe certificate replacement and revocation. | In deployment policy and the relevant trust boundary: manage control plane, queue, and certificate authority trust separately. | Run the security check in an isolated environment; verify this requirement: Manage control plane, queue, and certificate authority trust separately. | |
| [ ] Add certificate expiry monitoring | Alert before control plane and worker certificates expire. | Prevents avoidable service interruption. | In deployment policy and the relevant trust boundary: alert before control plane and worker certificates expire. | Run the security check in an isolated environment; verify this requirement: Alert before control plane and worker certificates expire. | |
| [ ] Review command allow rules | Define allowed executables, roots, users, and resource ceilings per worker pool. | Limits what a valid order can do. | In deployment policy and the relevant trust boundary: define allowed executables, roots, users, and resource ceilings per worker pool. | Run the security check in an isolated environment; verify this requirement: Define allowed executables, roots, users, and resource ceilings per worker pool. | |
| [ ] Add secret redaction | Remove secret values from logs, events, errors, and frontend responses. | Reduces credential disclosure. | In deployment policy and the relevant trust boundary: remove secret values from logs, events, errors, and frontend responses. | Run the security check in an isolated environment; verify this requirement: Remove secret values from logs, events, errors, and frontend responses. | |
| [ ] Add dependency vulnerability checks | Scan Go, npm, container, and release dependencies. | Detects known security risks before release. | In deployment policy and the relevant trust boundary: scan Go, npm, container, and release dependencies. | Run the security check in an isolated environment; verify this requirement: Scan Go, npm, container, and release dependencies. | |
| [ ] Add a security response process | Document revocation, rotation, incident evidence, and recovery actions. | Reduces response time after compromise. | In deployment policy and the relevant trust boundary: document revocation, rotation, incident evidence, and recovery actions. | Run the security check in an isolated environment; verify this requirement: Document revocation, rotation, incident evidence, and recovery actions. | |

Exit condition: network policy, identity checks, process restrictions, and secret handling pass a security review.

## Phase 11: Logs, metrics, alerts, and audit

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Add structured control plane logs | Include message, run, worker, key, and verification identifiers. | Makes distributed task traces searchable. | In the shared observability hooks and operator configuration: include message, run, worker, key, and verification identifiers. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Include message, run, worker, key, and verification identifiers. | |
| [ ] Add structured worker logs | Include local order state without secret values or full environments. | Supports VM troubleshooting without data leakage. | In the shared observability hooks and operator configuration: include local order state without secret values or full environments. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Include local order state without secret values or full environments. | |
| [ ] Add immutable security audit records | Record enrollment, key, permission, installer, cancellation, retry, and revocation actions. | Supports incident investigation and compliance review. | In the shared observability hooks and operator configuration: record enrollment, key, permission, installer, cancellation, retry, and revocation actions. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Record enrollment, key, permission, installer, cancellation, retry, and revocation actions. | |
| [ ] Measure due task count | Report producer backlog. | Shows schedule processing problems. | In the shared observability hooks and operator configuration: report producer backlog. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report producer backlog. | |
| [ ] Measure dispatch delay and outbox age | Report the time from due occurrence to confirmed publication. | Shows database or broker delivery problems. | In the shared observability hooks and operator configuration: report the time from due occurrence to confirmed publication. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report the time from due occurrence to confirmed publication. | |
| [ ] Measure event consumer delay | Report the age of unprocessed worker events. | Shows state update delays. | In the shared observability hooks and operator configuration: report the age of unprocessed worker events. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report the age of unprocessed worker events. | |
| [ ] Measure delivery and dead-letter counts | Report redelivery, exhaustion, and invalid message rates. | Shows reliability or attack problems. | In the shared observability hooks and operator configuration: report redelivery, exhaustion, and invalid message rates. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report redelivery, exhaustion, and invalid message rates. | |
| [ ] Measure signature rejections | Count failures by bounded reason and identity. | Detects configuration errors and forged traffic. | In the shared observability hooks and operator configuration: count failures by bounded reason and identity. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Count failures by bounded reason and identity. | |
| [ ] Measure worker heartbeat age and capacity | Report worker availability and active task count. | Supports scheduling and offline alerts. | In the shared observability hooks and operator configuration: report worker availability and active task count. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report worker availability and active task count. | |
| [ ] Measure task duration and final state | Report execution latency and outcomes. | Supports service health and task tuning. | In the shared observability hooks and operator configuration: report execution latency and outcomes. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Report execution latency and outcomes. | |
| [ ] Alert on stale outbox rows | Trigger when the oldest pending order exceeds policy. | Detects blocked task delivery. | In the shared observability hooks and operator configuration: trigger when the oldest pending order exceeds policy. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger when the oldest pending order exceeds policy. | |
| [ ] Alert on worker lease expiry | Trigger when a worker heartbeat expires. | Detects VM or network failure. | In the shared observability hooks and operator configuration: trigger when a worker heartbeat expires. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger when a worker heartbeat expires. | |
| [ ] Alert on repeated signature failure | Trigger on abnormal verification rejection rates. | Detects attack or key mismatch. | In the shared observability hooks and operator configuration: trigger on abnormal verification rejection rates. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger on abnormal verification rejection rates. | |
| [ ] Alert on dead-letter messages | Trigger when any message reaches a dead-letter subject. | Requires operator review of lost workflow progress. | In the shared observability hooks and operator configuration: trigger when any message reaches a dead-letter subject. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger when any message reaches a dead-letter subject. | |
| [ ] Alert on stuck task states | Trigger when a run exceeds its state timeout. | Detects incomplete lifecycle processing. | In the shared observability hooks and operator configuration: trigger when a run exceeds its state timeout. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger when a run exceeds its state timeout. | |
| [ ] Alert on stale resource leases | Trigger when a final run retains a resource. | Detects blocked future tasks. | In the shared observability hooks and operator configuration: trigger when a final run retains a resource. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Trigger when a final run retains a resource. | |
| [ ] Add external log storage only when required | Use self-hosted object storage when bounded messages cannot meet retention needs. | Defers an extra service until log volume requires it. | In the shared observability hooks and operator configuration: use self-hosted object storage when bounded messages cannot meet retention needs. | Trigger the condition and inspect the emitted telemetry; verify this requirement: Use self-hosted object storage when bounded messages cannot meet retention needs. | |

Exit condition: operators can trace one run from schedule through final state and receive alerts for each critical delay.

## Phase 12: Availability, backup, and operations

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Deploy multiple control plane replicas | Run active replicas coordinated by PostgreSQL locks. | Removes one control plane process as a single point of failure. | In deployment configuration and operator runbooks: run active replicas coordinated by PostgreSQL locks. | Run the matching failover or restore drill; verify this requirement: Run active replicas coordinated by PostgreSQL locks. | |
| [ ] Deploy a three-node JetStream cluster | Replicate streams across separate failure zones. | Preserves message delivery after one queue node fails. | In deployment configuration and operator runbooks: replicate streams across separate failure zones. | Run the matching failover or restore drill; verify this requirement: Replicate streams across separate failure zones. | |
| [ ] Define PostgreSQL high availability | Use Patroni and etcd for production failover when required. | Reduces central state downtime. | In deployment configuration and operator runbooks: use Patroni and etcd for production failover when required. | Run the matching failover or restore drill; verify this requirement: Use Patroni and etcd for production failover when required. | |
| [ ] Configure PostgreSQL backups | Use pgBackRest with point-in-time recovery. | Protects schedules, runs, public keys, and audit data. | In deployment configuration and operator runbooks: use pgBackRest with point-in-time recovery. | Run the matching failover or restore drill; verify this requirement: Use pgBackRest with point-in-time recovery. | |
| [ ] Exclude plaintext private keys from backups | Back up public state without unprotected signing keys. | Limits key exposure through backup systems. | In deployment configuration and operator runbooks: back up public state without unprotected signing keys. | Run the matching failover or restore drill; verify this requirement: Back up public state without unprotected signing keys. | |
| [ ] Test database failover | Move the primary during producer and event processing. | Proves lock and retry behavior. | In deployment configuration and operator runbooks: move the primary during producer and event processing. | Run the matching failover or restore drill; verify this requirement: Move the primary during producer and event processing. | |
| [ ] Test database restoration | Restore to a new environment and verify task and audit consistency. | Proves backups can recover the platform. | In deployment configuration and operator runbooks: restore to a new environment and verify task and audit consistency. | Run the matching failover or restore drill; verify this requirement: Restore to a new environment and verify task and audit consistency. | |
| [ ] Test JetStream replica loss | Remove a queue node during delivery. | Proves message availability. | In deployment configuration and operator runbooks: remove a queue node during delivery. | Run the matching failover or restore drill; verify this requirement: Remove a queue node during delivery. | |
| [ ] Define recovery objectives | Set recovery time and recovery point targets for each component. | Gives operations measurable service goals. | In deployment configuration and operator runbooks: set recovery time and recovery point targets for each component. | Run the matching failover or restore drill; verify this requirement: Set recovery time and recovery point targets for each component. | |
| [ ] Write operator runbooks | Document outages, stuck runs, dead letters, rotation, revocation, and restore. | Gives operators tested response steps. | In deployment configuration and operator runbooks: document outages, stuck runs, dead letters, rotation, revocation, and restore. | Run the matching failover or restore drill; verify this requirement: Document outages, stuck runs, dead letters, rotation, revocation, and restore. | |

Exit condition: tested failover and restore procedures meet the defined recovery objectives.

## Phase 13: Test plan

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Add unit tests | Test schedule rules, validators, transitions, selectors, limits, and retry policy. | Detects logic regressions quickly. | In the smallest relevant automated test package: test schedule rules, validators, transitions, selectors, limits, and retry policy. | Run the named test suite; verify this requirement: Test schedule rules, validators, transitions, selectors, limits, and retry policy. | |
| [ ] Add PostgreSQL integration tests | Test locks, uniqueness, transactions, outbox, inbox, and leases. | Proves central consistency rules. | In the smallest relevant automated test package: test locks, uniqueness, transactions, outbox, inbox, and leases. | Run the named test suite; verify this requirement: Test locks, uniqueness, transactions, outbox, inbox, and leases. | |
| [ ] Add JetStream integration tests | Test redelivery, acknowledgements, durability, limits, and dead letters. | Proves queue behavior. | In the smallest relevant automated test package: test redelivery, acknowledgements, durability, limits, and dead letters. | Run the named test suite; verify this requirement: Test redelivery, acknowledgements, durability, limits, and dead letters. | |
| [ ] Add worker execution tests | Test success, failure, timeout, cancellation, child processes, paths, limits, and output bounds. | Proves safe process handling. | In the smallest relevant automated test package: test success, failure, timeout, cancellation, child processes, paths, limits, and output bounds. | Run the named test suite; verify this requirement: Test success, failure, timeout, cancellation, child processes, paths, limits, and output bounds. | |
| [ ] Add enrollment tests | Test expiry, second use, wrong runner, wrong platform, and changed artifacts. | Proves bootstrap security. | In the smallest relevant automated test package: test expiry, second use, wrong runner, wrong platform, and changed artifacts. | Run the named test suite; verify this requirement: Test expiry, second use, wrong runner, wrong platform, and changed artifacts. | |
| [ ] Add queue permission tests | Deny cross-worker subscriptions and publications. | Proves worker isolation. | In the smallest relevant automated test package: deny cross-worker subscriptions and publications. | Run the named test suite; verify this requirement: Deny cross-worker subscriptions and publications. | |
| [ ] Add mutual TLS tests | Deny missing, expired, revoked, and wrong-identity certificates. | Proves transport authentication. | In the smallest relevant automated test package: deny missing, expired, revoked, and wrong-identity certificates. | Run the named test suite; verify this requirement: Deny missing, expired, revoked, and wrong-identity certificates. | |
| [ ] Add secret leakage tests | Scan logs, responses, events, installers, and state files. | Prevents accidental secret disclosure. | In the smallest relevant automated test package: scan logs, responses, events, installers, and state files. | Run the named test suite; verify this requirement: Scan logs, responses, events, installers, and state files. | |
| [ ] Add API authorization tests | Test every protected operation for each role. | Prevents permission regressions. | In the smallest relevant automated test package: test every protected operation for each role. | Run the named test suite; verify this requirement: Test every protected operation for each role. | |
| [ ] Add frontend workflow tests | Test enrollment, task creation, manual runs, history, cancellation, and retry. | Protects the main user workflows. | In the smallest relevant automated test package: test enrollment, task creation, manual runs, history, cancellation, and retry. | Run the named test suite; verify this requirement: Test enrollment, task creation, manual runs, history, cancellation, and retry. | |
| [ ] Add end-to-end tests | Run one task from the React UI through a remote worker and back. | Proves all components work together. | In the smallest relevant automated test package: run one task from the React UI through a remote worker and back. | Run the named test suite; verify this requirement: Run one task from the React UI through a remote worker and back. | |
| [ ] Add duplicate delivery tests | Publish each order and event twice. | Proves idempotent processing. | In the smallest relevant automated test package: publish each order and event twice. | Run the named test suite; verify this requirement: Publish each order and event twice. | |
| [ ] Add restart tests | Restart each component at every durable handoff. | Proves crash recovery. | In the smallest relevant automated test package: restart each component at every durable handoff. | Run the named test suite; verify this requirement: Restart each component at every durable handoff. | |
| [ ] Add load tests | Measure due tasks, dispatch, events, workers, and run history at target scale. | Finds capacity limits before production. | In the smallest relevant automated test package: measure due tasks, dispatch, events, workers, and run history at target scale. | Run the named test suite; verify this requirement: Measure due tasks, dispatch, events, workers, and run history at target scale. | |
| [ ] Add clock-skew tests | Test accepted tolerance and rejection beyond policy. | Proves time-based security checks. | In the smallest relevant automated test package: test accepted tolerance and rejection beyond policy. | Run the named test suite; verify this requirement: Test accepted tolerance and rejection beyond policy. | |

Exit condition: automated tests cover protocol, delivery, state, security, recovery, API, frontend, and end-to-end execution.

## Phase 14: Open-source supply chain and releases

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Record SPDX identifiers | Record a license identifier for each direct dependency. | Makes license review automatic. | In CI, release tooling, and release documentation: record a license identifier for each direct dependency. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Record a license identifier for each direct dependency. | |
| [ ] Enforce the approved license list | Reject unknown or disallowed dependency licenses in continuous integration. | Prevents incompatible components from entering releases. | In CI, release tooling, and release documentation: reject unknown or disallowed dependency licenses in continuous integration. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Reject unknown or disallowed dependency licenses in continuous integration. | |
| [ ] Review transitive licenses | Review dependency license changes before each release. | Reduces legal and redistribution risk. | In CI, release tooling, and release documentation: review dependency license changes before each release. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Review dependency license changes before each release. | |
| [ ] Generate a software bill of materials | Create an SBOM for control plane, worker, frontend, and installer artifacts. | Gives operators a complete component inventory. | In CI, release tooling, and release documentation: create an SBOM for control plane, worker, frontend, and installer artifacts. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Create an SBOM for control plane, worker, frontend, and installer artifacts. | |
| [ ] Sign release artifacts | Sign executables, containers, installers, checksums, and SBOM files. | Detects changed release files. | In CI, release tooling, and release documentation: sign executables, containers, installers, checksums, and SBOM files. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Sign executables, containers, installers, checksums, and SBOM files. | |
| [ ] Publish reproducible build instructions | Document tool versions and release commands. | Lets contributors verify official artifacts. | In CI, release tooling, and release documentation: document tool versions and release commands. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Document tool versions and release commands. | |
| [ ] Confirm no proprietary dependency | Test that deployment works without proprietary services or closed-source agents. | Preserves the open-source project goal. | In CI, release tooling, and release documentation: test that deployment works without proprietary services or closed-source agents. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Test that deployment works without proprietary services or closed-source agents. | |
| [ ] Publish protocol documentation | Document subjects, envelopes, payloads, signatures, limits, and version policy. | Lets other open-source tools interoperate safely. | In CI, release tooling, and release documentation: document subjects, envelopes, payloads, signatures, limits, and version policy. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Document subjects, envelopes, payloads, signatures, limits, and version policy. | |
| [ ] Publish deployment documentation | Document development, single-node, and production topologies. | Helps operators select an appropriate setup. | In CI, release tooling, and release documentation: document development, single-node, and production topologies. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Document development, single-node, and production topologies. | |
| [ ] Publish security documentation | Document trust, keys, enrollment, isolation, limitations, and response. | Sets correct security expectations. | In CI, release tooling, and release documentation: document trust, keys, enrollment, isolation, limitations, and response. | Build a release in CI and inspect its metadata and artifacts; verify this requirement: Document trust, keys, enrollment, isolation, limitations, and response. | |

Exit condition: every release is signed, documented, license-approved, and accompanied by an SBOM.

## Phase 15: Migration and production rollout

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Split the local producer and consumer behavior | Move scheduling and central writes to the control plane. Move execution to the worker. | Removes shared local database access. | In migration tooling and the rollout runbook: move scheduling and central writes to the control plane. Move execution to the worker. | Run the migration step in staging and compare old and new behavior; verify this requirement: Move scheduling and central writes to the control plane. Move execution to the worker. | |
| [ ] Remove email and history writes from workers | Process notifications and final history in the control plane. | Keeps central credentials on central hosts. | In migration tooling and the rollout runbook: process notifications and final history in the control plane. | Run the migration step in staging and compare old and new behavior; verify this requirement: Process notifications and final history in the control plane. | |
| [ ] Add a routing flag | Assign each task definition to either the local scheduler or Glyphflow during migration. | Prevents both systems from executing the same task. | In migration tooling and the rollout runbook: assign each task definition to either the local scheduler or Glyphflow during migration. | Run the migration step in staging and compare old and new behavior; verify this requirement: Assign each task definition to either the local scheduler or Glyphflow during migration. | |
| [ ] Import task definitions | Convert local schedules, commands, directories, resources, and runner identifiers. | Preserves existing task configuration. | In migration tooling and the rollout runbook: convert local schedules, commands, directories, resources, and runner identifiers. | Run the migration step in staging and compare old and new behavior; verify this requirement: Convert local schedules, commands, directories, resources, and runner identifiers. | |
| [ ] Validate imported schedules | Compare next occurrences between the local scheduler and Glyphflow. | Detects migration timing errors. | In migration tooling and the rollout runbook: compare next occurrences between the local scheduler and Glyphflow. | Run the migration step in staging and compare old and new behavior; verify this requirement: Compare next occurrences between the local scheduler and Glyphflow. | |
| [ ] Register one canary worker | Use one production-like VM and a small task group. | Limits initial deployment risk. | In migration tooling and the rollout runbook: use one production-like VM and a small task group. | Run the migration step in staging and compare old and new behavior; verify this requirement: Use one production-like VM and a small task group. | |
| [ ] Verify canary state and recovery | Check messages, runs, events, logs, metrics, duplicates, and restart behavior. | Proves the production path before expansion. | In migration tooling and the rollout runbook: check messages, runs, events, logs, metrics, duplicates, and restart behavior. | Run the migration step in staging and compare old and new behavior; verify this requirement: Check messages, runs, events, logs, metrics, duplicates, and restart behavior. | |
| [ ] Stop on duplicate execution | Define duplicate execution as a canary stop condition. | Protects systems with external side effects. | In migration tooling and the rollout runbook: define duplicate execution as a canary stop condition. | Run the migration step in staging and compare old and new behavior; verify this requirement: Define duplicate execution as a canary stop condition. | |
| [ ] Expand task groups gradually | Move tasks only after stable canary periods. | Limits the effect of unknown failures. | In migration tooling and the rollout runbook: move tasks only after stable canary periods. | Run the migration step in staging and compare old and new behavior; verify this requirement: Move tasks only after stable canary periods. | |
| [ ] Test rollback | Return routed tasks to the local scheduler without duplicate occurrences. | Provides a safe exit from the migration. | In migration tooling and the rollout runbook: return routed tasks to the local scheduler without duplicate occurrences. | Run the migration step in staging and compare old and new behavior; verify this requirement: Return routed tasks to the local scheduler without duplicate occurrences. | |
| [ ] Retire local queue access | Remove local consumer access after all tasks migrate. | Completes the network architecture boundary. | In migration tooling and the rollout runbook: remove local consumer access after all tasks migrate. | Run the migration step in staging and compare old and new behavior; verify this requirement: Remove local consumer access after all tasks migrate. | |
| [ ] Complete the production readiness review | Verify all completion gates below. | Prevents an incomplete security or recovery model from reaching production. | In migration tooling and the rollout runbook: verify all completion gates below. | Run the migration step in staging and compare old and new behavior; verify this requirement: Verify all completion gates below. | |

Exit condition: all routed tasks use Glyphflow, and the local consumer cannot execute them.

## Production completion gates

| Gate | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] No worker can connect to PostgreSQL | Verify packages, configuration, credentials, routes, and firewall rules. | Protects central data from worker compromise. | In production-readiness automation and its runbook: verify packages, configuration, credentials, routes, and firewall rules. | Run the production-readiness check; verify this requirement: Verify packages, configuration, credentials, routes, and firewall rules. | |
| [ ] Every order has a valid control plane signature | Reject all unsigned, altered, expired, or revoked-key orders. | Prevents unauthorized execution. | In production-readiness automation and its runbook: reject all unsigned, altered, expired, or revoked-key orders. | Run the production-readiness check; verify this requirement: Reject all unsigned, altered, expired, or revoked-key orders. | |
| [ ] Every accepted event has a valid worker signature | Reject all unsigned, altered, stale, or wrong-worker events. | Protects task state integrity. | In production-readiness automation and its runbook: reject all unsigned, altered, stale, or wrong-worker events. | Run the production-readiness check; verify this requirement: Reject all unsigned, altered, stale, or wrong-worker events. | |
| [ ] Mutual TLS protects every queue connection | Require valid control plane and worker certificates. | Protects transport identity and confidentiality. | In production-readiness automation and its runbook: require valid control plane and worker certificates. | Run the production-readiness check; verify this requirement: Require valid control plane and worker certificates. | |
| [ ] Queue permissions isolate every worker | Test publish and subscribe denial for other worker subjects. | Prevents cross-worker access. | In production-readiness automation and its runbook: test publish and subscribe denial for other worker subjects. | Run the production-readiness check; verify this requirement: Test publish and subscribe denial for other worker subjects. | |
| [ ] Duplicate orders execute once | Verify persistent worker message uniqueness. | Reduces duplicate command effects. | In production-readiness automation and its runbook: verify persistent worker message uniqueness. | Run the production-readiness check; verify this requirement: Verify persistent worker message uniqueness. | |
| [ ] Duplicate events apply once | Verify central event uniqueness and transaction rules. | Preserves correct task state. | In production-readiness automation and its runbook: verify central event uniqueness and transaction rules. | Run the production-readiness check; verify this requirement: Verify central event uniqueness and transaction rules. | |
| [ ] Both outboxes recover after failure | Test control plane order and worker event publication recovery. | Prevents lost work and results. | In production-readiness automation and its runbook: test control plane order and worker event publication recovery. | Run the production-readiness check; verify this requirement: Test control plane order and worker event publication recovery. | |
| [ ] Key rotation and revocation pass | Test overlap, expiry, immediate rejection, and certificate removal. | Supports routine and emergency key operations. | In production-readiness automation and its runbook: test overlap, expiry, immediate rejection, and certificate removal. | Run the production-readiness check; verify this requirement: Test overlap, expiry, immediate rejection, and certificate removal. | |
| [ ] Every run has a complete audit history | Trace schedule, order, events, state, user actions, and verification results. | Supports operations and incident review. | In production-readiness automation and its runbook: trace schedule, order, events, state, user actions, and verification results. | Run the production-readiness check; verify this requirement: Trace schedule, order, events, state, user actions, and verification results. | |
| [ ] The primary worker setup uses one download | Verify the administrator enrollment workflow. | Makes secure deployment practical. | In production-readiness automation and its runbook: verify the administrator enrollment workflow. | Run the production-readiness check; verify this requirement: Verify the administrator enrollment workflow. | |
| [ ] Private keys originate on the worker | Inspect installers and enrollment traffic. | Prevents central possession of worker private keys. | In production-readiness automation and its runbook: inspect installers and enrollment traffic. | Run the production-readiness check; verify this requirement: Inspect installers and enrollment traffic. | |
| [ ] Enrollment tokens expire and work once | Test expiry, claim races, replay, and wrong-runner use. | Prevents worker identity theft. | In production-readiness automation and its runbook: test expiry, claim races, replay, and wrong-runner use. | Run the production-readiness check; verify this requirement: Test expiry, claim races, replay, and wrong-runner use. | |
| [ ] All required software has an approved license | Validate direct and transitive dependencies. | Preserves legal open-source distribution. | In production-readiness automation and its runbook: validate direct and transitive dependencies. | Run the production-readiness check; verify this requirement: Validate direct and transitive dependencies. | |
| [ ] Every release includes SPDX data and an SBOM | Publish and verify release metadata. | Gives operators supply-chain visibility. | In production-readiness automation and its runbook: publish and verify release metadata. | Run the production-readiness check; verify this requirement: Publish and verify release metadata. | |
| [ ] Deployment requires no proprietary endpoint | Install and operate the complete platform with self-hosted components. | Preserves vendor independence. | In production-readiness automation and its runbook: install and operate the complete platform with self-hosted components. | Run the production-readiness check; verify this requirement: Install and operate the complete platform with self-hosted components. | |
| [ ] Backup and restore tests pass | Recover central state and confirm audit consistency. | Proves disaster recovery. | In production-readiness automation and its runbook: recover central state and confirm audit consistency. | Run the production-readiness check; verify this requirement: Recover central state and confirm audit consistency. | |
| [ ] Failure and canary tests pass | Complete outage, duplicate, restart, revocation, and rollout checks. | Proves production readiness. | In production-readiness automation and its runbook: complete outage, duplicate, restart, revocation, and rollout checks. | Run the production-readiness check; verify this requirement: Complete outage, duplicate, restart, revocation, and rollout checks. | |

## Deferred work

| Task | Description | Impact | How to implement | How to test | Test Result |
|---|---|---|---|---|---|
| [ ] Evaluate service splitting | Split scheduler, dispatcher, ingester, or notifications only after measured scaling or ownership needs. | Avoids early distributed-system complexity. | Only after the stated trigger is measured, in the owning component: split scheduler, dispatcher, ingester, or notifications only after measured scaling or ownership needs. | Run a focused proof of concept and regression check; verify this requirement: Split scheduler, dispatcher, ingester, or notifications only after measured scaling or ownership needs. | |
| [ ] Evaluate container task isolation | Add containers only when operating system accounts cannot meet isolation requirements. | Avoids an unnecessary runtime dependency. | Only after the stated trigger is measured, in the owning component: add containers only when operating system accounts cannot meet isolation requirements. | Run a focused proof of concept and regression check; verify this requirement: Add containers only when operating system accounts cannot meet isolation requirements. | |
| [ ] Evaluate external log storage | Add self-hosted object storage only when bounded queue messages cannot meet retention needs. | Avoids operating unused storage infrastructure. | Only after the stated trigger is measured, in the owning component: add self-hosted object storage only when bounded queue messages cannot meet retention needs. | Run a focused proof of concept and regression check; verify this requirement: Add self-hosted object storage only when bounded queue messages cannot meet retention needs. | |
| [ ] Evaluate automatic cross-worker reassignment | Add it only after task owners define retry safety and idempotency. | Avoids duplicate external effects. | Only after the stated trigger is measured, in the owning component: add it only after task owners define retry safety and idempotency. | Run a focused proof of concept and regression check; verify this requirement: Add it only after task owners define retry safety and idempotency. | |
| [ ] Evaluate hardware attestation | Add it only when the threat model requires stronger worker trust. | Avoids complex security infrastructure without a defined need. | Only after the stated trigger is measured, in the owning component: add it only when the threat model requires stronger worker trust. | Run a focused proof of concept and regression check; verify this requirement: Add it only when the threat model requires stronger worker trust. | |
